{
  "name": "team-task-manager-deploy",
  "version": "1.0.0",
  "description": "Root package for Railway Deployment",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && npm run build && mv dist ../backend/public && cd ../backend && npm install",
    "start": "cd backend && npm start"
  }
}
